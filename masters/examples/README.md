# Example Master Packages

This directory stores tracked snapshots for curated examples.

- `su-shi/`
- `han-yu/`

Each example includes:
- `package/` canonical files generated under `masters/{slug}`
- `runtime/` exported files generated under `.claude/skills/{slug}`
